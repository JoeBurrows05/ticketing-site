services:
    gc-tickets:
      image: node:22
      container_name: gc-tickets
      working_dir: /app
      ports:
        - "3000:3000"
      volumes:
          - .:/app
          - /app/node_modules
      command: sh -c "npm install && npm run dev"
